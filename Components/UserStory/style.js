import { StyleSheet } from "react-native";

const style = StyleSheet.create({
    storyContainer: {
        alignItems: 'center',
        marginHorizontal: 10,
        marginRight: 20,
    },

    firstName: {
        fontSize: 14,
        marginTop: 8,
        color: '#022150',
        textAlign: 'center',
    },
});

export default style;
